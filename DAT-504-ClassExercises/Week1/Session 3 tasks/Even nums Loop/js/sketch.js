//Loop
for (let num = 1; num <=10; num++){ //While num is less than 10 (and once equals 10) add num to itself
  if(num % 2 == 0){ //If the remainder of 'num' when divded by '2' is equal to '0', value is even
    console.log(num);
  }
}
